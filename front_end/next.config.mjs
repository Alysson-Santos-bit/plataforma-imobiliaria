/** @type {import('next').NextConfig} */
const nextConfig = {
  // Por enquanto, não precisamos de configurações personalizadas.
  // No futuro, podemos adicionar configurações para otimização de imagens,
  // redirecionamentos, etc.
};

export default nextConfig;
